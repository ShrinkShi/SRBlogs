# 2026-05-04 页面配置与透明度闭环

- [ ] `GET /api/pages/config` 返回 200。
- [ ] 未登录 `GET /api/admin/pages/config` 返回 401，不是 404。
- [ ] 页面编辑 > 首页 保存组件顺序后，刷新前台首页真实变化。
- [ ] 页面编辑 > 首页 保存组件宽度/高度后，刷新前台首页真实变化。
- [ ] 恢复默认布局后，刷新前台首页恢复默认。
- [ ] 后台“主题与背景 > 前台透明度设置”可调整工具箱设置弹窗透明度，前台刷新后生效。
- [ ] 后台“主题与背景 > 前台透明度设置”可调整全局搜索弹窗透明度，前台刷新后生效。
- [ ] 左下角工具箱悬浮球、工具菜单项、弹窗关闭按钮点击有音效。
- [ ] 空白点击只有视觉特效，不播放音效。
- [ ] 构建产物 Secret 静态搜索无命中。

# 2026-05-04 Component Theme And Music Likes QA

- [ ] 后台透明度滑块和数字输入允许 `0..1`，并提示 `0` 可能导致组件不可见。
- [ ] 后台组件样式 DIY 中能看到顶部导航栏、工具箱、Toast、首页组件、内容卡片、音乐面板、留言板、搜索框、标签按钮等中文组件名。
- [ ] 修改工具箱设置弹窗日间/夜间颜色和透明度后，前台刷新生效。
- [ ] 修改工具箱全局搜索弹窗日间/夜间颜色和透明度后，前台刷新生效。
- [ ] 修改 Toast 颜色和透明度后，提示层仍可读且不挤占布局。
- [ ] 修改顶部导航栏颜色、透明度、大小后，前台刷新生效且移动端不炸。
- [ ] 修改正经/杂谈切换按钮、矩阵网格/中枢链路切换按钮、搜索框、搜索按钮、标签按钮样式后，前台刷新生效。
- [ ] 页面编辑 > 首页 每个组件卡片显示 order、w、h、透明度、日间背景、夜间背景、大小、visible 状态。
- [ ] 首页播放器播放模式按钮可在顺序播放、随机播放、单曲循环之间切换。
- [ ] 音乐页播放器播放模式按钮与首页共享状态。
- [ ] 顺序播放到列表末尾后回到第一首。
- [ ] 随机播放尽量不连续重复当前歌曲。
- [ ] 单曲循环在歌曲结束后重复当前歌曲。
- [ ] 首页播放器喜欢按钮可增加/取消喜欢，喜欢数即时变化。
- [ ] 音乐页播放器喜欢按钮与首页当前歌曲状态同步。
- [ ] 同一浏览器刷新后已喜欢歌曲保持已喜欢状态。
- [ ] 音乐页歌单按 likes 降序排列，likes 相同时保持原有排序。
- [ ] 旧歌曲没有 likes 字段时按 0 展示。
- [ ] 390px 移动端播放模式、喜欢按钮、组件样式不挤爆布局。
- [ ] GitHub/QQ 留言、照片墙相册、文章页、页面编辑布局保存不回退。

# Manual QA Checklist

## 2026-05-03 首页页面编辑硬修复验收

- [ ] 工具箱“设置”弹窗面板本体明显更不透明，背景只轻微透出，不影响阅读。
- [ ] 工具箱“全局搜索”弹窗面板和搜索结果卡片明显更不透明，日间/夜间都清晰。
- [ ] 鼠标点击视觉特效在页面任意点击时触发。
- [ ] 点击音效只在 button、a、role=button、data-clickable、自定义按钮、导航项、工具箱菜单项、保存/上传/搜索/关闭按钮等交互元素触发。
- [ ] 点击空白区域没有点击音效。
- [ ] 字体大小“小 / 中 / 大”只改变文字大小，不改变卡片、图片、唱片、轮播图、弹窗、列宽和布局比例。
- [ ] Markdown 正文、页面标题、导航、按钮、标签、留言板和工具箱弹窗文字都随字体档位变化。
- [ ] 页面编辑 > 首页 拆分为 8 个真实模块：名片、音乐播放器、歌词区、最新文章轮播、图片轮播、更新内容轮播、昼夜切换、底部状态区。
- [ ] 页面编辑 > 首页 修改作者、头像、简介、社交链接并保存后，刷新前台首页资料卡真实生效。
- [ ] 页面编辑 > 首页 调整模块顺序并保存后，`GET /api/pages/config` 返回新顺序，刷新前台首页真实变化。
- [ ] 页面编辑 > 首页 调整模块宽度/高度并保存后，刷新前台首页真实变化。
- [ ] 页面编辑 > 首页 恢复默认布局并保存后，刷新前台首页恢复默认顺序和尺寸。
- [ ] 页面编辑 > 关于 读取真实 about Markdown，保存后前台关于页更新。
- [ ] 设置中心“站点公开信息”不再把作者、头像、首页简介、社交链接作为主流程字段；这些字段主入口在 页面编辑 > 首页。

## 2026-05-03 页面编辑真实绑定专项验收

- [ ] 工具箱设置弹窗背景明显更不透明，正文、滑块、选择器和按钮在日间/夜间均清晰可读。
- [ ] 工具箱全局搜索弹窗背景明显更不透明，结果卡片不被背景干扰。
- [ ] 鼠标点击视觉特效在页面任意点击时触发，包括空白区域。
- [ ] 点击音效只在按钮、链接、导航项、表单操作、工具箱菜单等交互元素点击时触发，空白区域不播放声音。
- [ ] 后台关闭点击特效后，前台刷新后视觉点击特效完全关闭，游客设置不可强行开启。
- [ ] 后台关闭点击音效后，前台刷新后点击音效完全关闭，游客设置不可强行开启。
- [ ] 字体大小“小 / 中 / 大”对顶部导航、页面标题、文章正文、Markdown、卡片、按钮、标签、输入框、留言板、工具箱弹窗都有明显影响。
- [ ] 文章正经板块、杂谈板块、图片页的中枢链路上下卡片间距约为原先 2.5 倍，左右交替和贴近中心线保持正常。
- [ ] 后台不再出现中间重复小导航栏，左侧主导航是唯一主导航。
- [ ] “页面编辑 > 首页”读取真实作者、头像 URL、简介、社交链接和首页布局配置。
- [ ] 修改首页作者、头像 URL、简介或社交链接并保存后，刷新前台首页资料卡生效。
- [ ] “页面编辑 > 首页”预览区核心模块可拖拽位置、拖动右下角缩放，保存后刷新前台首页能看到布局变化。
- [ ] “页面编辑 > 文章”读取并保存文章板块标题/副标题和杂谈板块标题/副标题。
- [ ] “页面编辑 > 图片 / 音乐 / 项目 / 友链”读取真实页面文案和对应数据数量摘要。
- [ ] “页面编辑 > 关于”读取真实 about Markdown，保存后前台关于页更新。
- [ ] 页面编辑中的新增文章、相册、歌曲、项目、友链入口复用原内容管理入口，原内容管理入口仍可用。
- [ ] 设置中心“站点公开信息”不再把作者、头像、首页简介、社交链接作为主流程；这些字段主入口迁移到页面编辑首页。

## 2026-05-03 工具箱与后台信息架构回归

- [ ] 工具箱设置弹窗背景更不透明，日间/夜间模式下文字和控件清晰可读。
- [ ] 工具箱全局搜索弹窗背景更不透明，搜索、筛选、结果跳转、空状态和错误状态可用。
- [ ] 计算器以小浮窗显示，不出现全屏遮罩，不阻塞页面滚动和其他按钮点击。
- [ ] 计算器支持四则运算、括号、小数、清空、退格和中文错误提示。
- [ ] 点击视觉特效响应任意页面点击；点击音效仍只响应按钮、链接、导航项、工具箱菜单项等交互元素。
- [ ] 后台关闭鼠标点击特效后，前台刷新不再显示点击特效，游客设置中显示站点已关闭或不可用。
- [ ] 顶部导航桌面端页面间距加大，1280/1024/390 宽度不挤爆。
- [ ] 游客字体大小小/中/大三档对导航、卡片、按钮、留言板、工具箱弹窗等前台文字有明显影响。
- [ ] 文章正经板块、杂谈板块和图片页的中枢链路上下卡片间隔更疏朗。
- [ ] 后台一级导航显示为页面编辑、内容管理、评论管理、后台设置、日志备份。
- [ ] 页面编辑菜单可进入首页、文章、图片、音乐、项目、友链、关于。
- [ ] 页面编辑页可修改标题、副标题、说明文案，并保存到页面布局配置。
- [ ] 页面编辑预览区组件可拖动位置、拖动边缘改变大小；未保存前只影响后台预览。
- [ ] 页面编辑可恢复默认布局。
- [ ] 内容管理菜单可进入写作编辑器、文章管理、草稿、动态、杂谈、媒体下的友链/项目/音乐/图片。
- [ ] 评论管理入口显示为留言管理，旧 `/admin/comments` 仍可访问。
- [ ] 后台设置入口可进入站点公开信息、主题与背景、留言设置、图床设置、AI 设置、部署与安全提示。
- [ ] 日志备份入口可进入总览、审计日志、备份恢复。
- [ ] 旧后台路由 `/admin/editor`、`/admin/posts`、`/admin/settings`、`/admin/audit`、`/admin/backups` 不 404。
- [ ] 操作暂存区默认完全隐藏，不占主操作区宽度；点击右下角按钮后可打开并再次关闭。
- [ ] 390px 下工具箱和后台侧栏仍可操作。

## 2026-05-03 前台导航、文章整合、图片双模式与工具箱验收

- [ ] 顶部导航顺序为：首页、文章、图片、音乐、项目、友链、关于。
- [ ] 顶部导航不再显示搜索、杂谈、归档入口。
- [ ] “图片”入口进入 `/photowall`，旧 `/photowall` 路由不 404。
- [ ] `/search` 旧路由仍可直接访问，但不在顶部导航展示。
- [ ] `/chatters` 旧路由不 404，并能进入杂谈兼容入口或杂谈板块。
- [ ] `/posts` 默认展示“正经”板块，读取公开 posts 数据，不显示草稿。
- [ ] `/posts?section=chatters` 展示“杂谈”板块，读取公开 chatters 数据。
- [ ] 正经/杂谈切换开关在桌面和 390px 移动端都可操作，不挤爆布局。
- [ ] 正经板块的矩阵网格和中枢链路均可切换，文章详情跳转正常。
- [ ] 杂谈板块的矩阵网格和中枢链路均可切换，杂谈详情跳转正常。
- [ ] 图片页矩阵网格展示相册封面，点击封面仍能打开相册全部照片。
- [ ] 图片页中枢链路左右交替、贴近中心线，移动端退化为单列。
- [ ] 左下角工具箱悬浮球可见，点击后显示计算器、全局搜索、设置。
- [ ] 工具箱点击空白处或 Esc 可关闭菜单/弹窗。
- [ ] 计算器支持加减乘除、括号、小数、清空、退格，错误表达式显示中文错误。
- [ ] 全局搜索弹窗支持关键词、类型筛选、标签筛选、结果跳转、加载/空/错误状态。
- [ ] 游客设置弹窗可控制昼夜、主题、背景、氛围、弹幕、点击音效、音乐音量和字体大小，并立即生效。
- [ ] 首页右侧旧主题、背景、氛围、弹幕、点击音效按钮已删除，相关功能未丢失。
- [ ] 390px 移动端下工具箱、文章切换、图片双模式和搜索弹窗不产生严重横向溢出。

## 2026-05-03 GitHub/QQ 留言登录专项验收

- [ ] `/api/settings/public` 中 `comments.enabled=true`。
- [ ] `/api/settings/public` 中 `comments.providers.github.enabled=true`。
- [ ] `/api/settings/public` 中 `comments.providers.github.configured=true`。
- [ ] `/api/settings/public` 中 `comments.providers.qq.enabled=true`。
- [ ] `/api/settings/public` 中 `comments.providers.qq.configured=false` 时，GitHub 登录按钮仍可见。
- [ ] 前台留言板显示“使用 GitHub 登录后留言”按钮。
- [ ] QQ 未配置时只显示 QQ 未开启提示，不影响 GitHub。
- [ ] 点击 GitHub 登录按钮访问后端 `/api/auth/github/login?returnTo=当前路径`，不进入前端 404。
- [ ] `/api/auth/qq/login?returnTo=当前路径` 未配置时返回中文友好错误，不返回 Not Found。
- [ ] 未登录直接提交留言返回 401，中文提示“请先登录后再留言。”。
- [ ] 前台不显示 Secret、后端、`.env`、Client Secret 等开发者文案。

## 2026-05-03 多平台留言与中文化回归

- [ ] `/api/settings/public` 中 `comments.providers.github.configured` 与 `comments.providers.qq.configured` 均为布尔值，且不会返回任何 OAuth Secret。
- [ ] GitHub 已配置时，前台留言板显示“使用 GitHub 登录后留言”按钮；QQ 未配置时不影响 GitHub 按钮。
- [ ] GitHub/QQ 未配置提示均为中文访客文案，不出现后端、Secret、Client Secret、OAuth Secret、`.env`、Please try again later 等字样。
- [ ] 点击 GitHub/QQ 登录入口访问 `/api/auth/github/login` 或 `/api/auth/qq/login`，接口不返回 Not Found。
- [ ] 未登录直接提交留言返回 401，前台显示“请先登录后再留言。”。
- [ ] 后台设置页留言相关配置项中文展示，Secret 状态显示“已配置/未配置”，空 Secret 保存不覆盖旧值。
- [ ] 文章页、杂谈页、搜索页搜索框更紧凑、更不透明，右侧为深色搜索图标按钮，移动端不溢出。

## 2026-05-03 留言板入口、相册滚动、标题轻量化与音量控件验收
- [ ] 前台文章详情留言板可见“使用 GitHub 登录后留言”入口，入口出现在前台留言板区域而不是后台页面。
- [ ] 点击留言板 GitHub 登录入口会访问 `/api/auth/github/login?returnTo=当前前台路径`，授权后应回到原页面。
- [ ] 后台开启留言板但 GitHub OAuth 未配置时，前台显示访客友好提示；GitHub OAuth configured=true 时前台显示登录按钮。
- [ ] GitHub OAuth 未配置时，前台只显示“站点暂未开启 GitHub 留言，请稍后再试或联系站点管理员。”，不出现后端、Secret、`.env` 等开发者文案。
- [ ] 后台照片墙相册编辑弹窗在图片较多时可纵向滚动，底部保存/关闭按钮仍可操作。
- [ ] 文章矩阵卡片、文章中枢链路卡片、杂谈卡片和搜索结果卡片的标签区靠近卡片底部，不随摘要长度上下跳动。
- [ ] 文章页中枢链路模式单卡片约为内容区三分之一宽，贴靠中心线，结构为上半部分图片、下半部分时间/标题/摘要/标签。
- [ ] 文章页中枢链路模式恢复左右交替排列，不能全部堆在同一侧。
- [ ] 搜索框内不显示 `Search` 字样，右侧是深色搜索图标按钮，搜索区域外层仍无厚重背景。
- [ ] 搜索输入框本身恢复正常输入框样式；搜索区域和页面标题外层不再有厚重边框或背景填充。
- [ ] 除首页外主要页面标题/副标题居中，标题外层不再像大毛玻璃卡片。
- [ ] 首页播放器和音乐页播放器都有音量滑杆、静音/取消静音按钮，音量跨页面保持一致并写入 localStorage。
- [ ] 390px 下留言板、照片弹窗、文章双模式和播放器音量控件不产生严重横向溢出。
## P1/P2 混合收口轮

- [ ] Toast 出现在顶部顶层，不在主页底部，不挤占布局；复制成功为绿色，失败为红色/警告。
- [ ] 首页名片复制、后台保存、上传成功三个场景都触发同一个顶部 Toast。
- [ ] 后台左侧导航吸附最左侧并铺满上下；设置中心分区在“系统 > 设置中心”层级下可进入。
- [ ] Markdown 编辑器左右分栏无明显间距，中间分隔条可拖动；390px 下编辑/预览切换可用。
- [ ] Markdown 字体颜色按钮打开自定义颜色弹窗，有颜色选择器、输入框和预设色；不出现浏览器 prompt。
- [ ] 设置中心可配置点击音效启用、音量和音效 URL；前台只在交互元素点击时播放。
- [ ] 首页音乐播放后切换到文章、搜索、音乐页等路由，播放状态和进度不丢失。
- [ ] 音乐管理可上传 `.lrc`/`.txt` 歌词文件；首页歌词区优先展示当前歌曲歌词。
- [ ] 未配置 GitHub OAuth 时评论区显示未配置提示；未登录时不能评论；配置后只允许 GitHub 登录发表评论。
- [ ] GitHub 评论登录入口出现在前台文章详情评论区，而不是后台页面；登录成功后回到当前文章详情。
- [ ] 所有评论区文案统一为“留言板”；未配置 OAuth 时提示“站点暂未开启 GitHub 登录留言，请稍后再试或联系站点管理员。”
- [ ] 留言板未登录时只显示 GitHub 登录入口，不显示可提交的正式输入态；登录后显示头像、用户名、输入框和退出登录。
- [ ] 照片墙显示相册封面；点击相册可查看最多 50 张照片；后台可批量上传并删除相册内单张照片。
- [ ] 后台相册编辑可预览组内照片、增加照片、删除单张、拖动/按钮排序、设置封面。
- [ ] 名片统计和联系方式无边框无填充，仍可跳转/复制，复制反馈走顶层 Toast。
- [ ] 名片统计 hover 缩放明显；GitHub/QQ/微信/Email hover 变色并显示 tooltip。
- [ ] 首页轮播分页点 hover 即切换；移动端点击切换仍可用。
- [ ] 首页音乐播放器右上角无歌单按钮；右下角无冗余悬浮播放器。
- [ ] 首页音乐播放器唱片在左侧，歌名、歌手、进度条和按钮在右半部分居中，整体高度与名片接近。
- [ ] 首页歌词卡片不显示 `lyrics` 标签，高度紧凑，歌词单行居中且长句自动缩小。
- [ ] `/posts` 标题和副标题居中，搜索框无外边框，标签栏居中，文章卡片大屏三列且封面铺满上半部分。
- [ ] `/posts` 可在“矩阵网格 / 中枢链路”之间切换；中枢链路大屏左右交替、小屏单列。
- [ ] 前台顶部导航不再显示“归档”入口，但 `/archive` 仍可直接访问。
- [ ] 首页和音乐页播放器上一首、播放/暂停、下一首按钮无边框无填充，仅保留图标。
- [ ] 文章/杂谈卡片标签区沉到卡片底部，不紧贴摘要后。
- [ ] 大搜索栏无外边框和背景填充，日间/夜间模式均可读。
- [ ] `/chatters` 标题和副标题居中，搜索框无外边框，杂谈卡片大屏三列且封面铺满上半部分。
- [ ] `/music` 为左侧播放器、右侧歌词/歌单双页签；切歌后歌词同步，390px 下上下布局。
- [ ] 除首页外，主要页面标题和副标题居中显示，不影响正文阅读布局。

## 2026-05-03 P2 交互细节与后台弹窗化验收

- [ ] 复制成功 Toast 出现在顶部顶层，不在页面底部，不挤占布局。
- [ ] 保存成功、上传成功等提示可复用同一顶层 Toast。
- [ ] 后台设置页不再显示突兀的独立左侧二级导航卡片，设置分区切换为轻量顶部标签或等价形式。
- [ ] 主题与背景 token 配置展示中文名称、内部 key、颜色预览块、颜色选择器和文本输入框。
- [ ] 90MB 级音频上传不再被图片级限制拦截；超过 100MB 音频显示明确错误。
- [ ] 视频上传按 200MB 限制处理；非法类型返回 UI 错误。
- [ ] 新增/编辑友链、项目、音乐、照片使用居中弹窗，保存成功后关闭并刷新列表。
- [ ] 弹窗点击遮罩或 Esc 可关闭；有未保存内容时有确认。
- [ ] `/admin/editor` 默认显示打开 Markdown 编辑器入口，点击后出现近全屏弹窗。
- [ ] Markdown 弹窗桌面左右分栏，390px 下可切换编辑/预览。
- [ ] Markdown 快捷按钮栏可收起；加粗、斜体、列表、引用、代码、表格、链接、图片、字体颜色可用。
- [ ] 字体颜色插入后前台和后台预览均安全显示，不执行危险 HTML。
- [ ] 昼夜模式卡片图标和文字完全居中。
- [ ] 鼠标跟随巨大光晕已移除。
- [ ] 点击音效只在按钮、链接、表单项等交互元素点击时播放，空白点击不播放，并可关闭。
- [ ] 右下角冗余悬浮音乐播放器已移除，首页主播放器和 `/music` 页面不受影响。

## 2026-05-03 P2 第三阶段：轮播追平与后台结构修复验收

- [ ] `/admin/editor` 可打开，不再出现 `Failed to fetch dynamically imported module`。
- [ ] `/admin/about` 关于页 Markdown 编辑可打开，不再出现动态导入失败。
- [ ] 首页轮播区布局为：左侧文章轮播；右侧上层照片轮播；右侧下层更新轮播 + 独立昼夜切换卡片。
- [ ] 昼夜切换卡片不是轮播项，不显示轮播分页点。
- [ ] 三个轮播都能切换，图片铺满卡片，标题/摘要/时间/分页点位于图片内部。
- [ ] 有图轮播 hover 只放大图片层，不放大整张卡片。
- [ ] 名片、歌词、播放器、昼夜切换卡片、底部状态区 hover 为轻微放大，不以上移为主要效果。
- [ ] email/QQ/微信复制成功出现绿色提示，失败出现错误提示。
- [ ] 歌词区始终单行居中，长句自动缩小字号，短句恢复正常字号。
- [ ] 后台左侧导航为垂直父子目录结构，不再是胶囊式主导航。
- [ ] 右侧“操作暂存区”可完全隐藏，隐藏后中间主操作区变宽，并可恢复显示。
- [ ] 设置中心主题 token 展示为中文名称，颜色选择器和文本输入框都可用。
- [ ] 图片、音乐、视频字段可使用本地上传回填 URL；非法类型、超大文件有 UI 错误提示。
- [ ] 前台导航栏不再显示“后台入口”，其他导航项不受影响。
- [ ] 390px 首页和后台主要页面不严重横向溢出。

## 2026-05-03 P2 首页轮播与播放器人工验收

- [ ] 首页旧板块“内容发现、站点仪表盘、最新文章、云端杂谈、最近瞬间”不再出现在首页旧位置。
- [ ] 首页三组轮播真实可用：最新文章轮播、最新更新内容轮播、主题模式轮播。
- [ ] 最新文章轮播读取公开文章数据，草稿文章不出现。
- [ ] 最新更新内容轮播聚合公开 posts/moments/chatters，草稿内容不出现。
- [ ] 轮播分页点可切换，自动切换平滑且不突兀。
- [ ] 有图片轮播项 hover 时只放大图片，不放大整张卡片。
- [ ] 无图片轮播项 hover 时整张卡片轻微放大，不再以上移作为主要效果。
- [ ] 顶部导航下滚隐藏、上滚显示、鼠标到顶部显示逻辑不变，动画更慢更柔和。
- [ ] ProfileCard 符合“头像 + Shrink + 简介 / 文章、杂谈、照片统计 / GitHub、邮箱、QQ、微信图标”结构。
- [ ] 文章、杂谈、照片统计可点击跳转到 `/posts`、`/chatters`、`/photowall`。
- [ ] GitHub 图标打开 `https://github.com/ShrinkShi`；邮箱、QQ、微信复制成功/失败都有提示。
- [ ] 歌词区水平居中并在视觉上垂直居中；无歌词时显示居中的歌曲信息或占位文案。
- [ ] 首页音乐播放器符合唱片 + 歌名 + 歌手 + 进度条 + 三个图标按钮结构。
- [ ] 有歌曲 URL 时播放/暂停可用；无 URL 时不报错并显示不可播放提示。
- [ ] 1440/1280/1024/768/390 宽度下首页不严重溢出、不裁切、不撑宽。
- [ ] 搜索、标签、归档、媒体页和后台 settings 未被本轮首页 P2 调整破坏。

本清单用于浏览器人工回归。除非全部相关项通过，否则不要把 `docs/XINGHUI_PARITY_MATRIX.md` 中对应模块标记为 `已完成`。

## P2 视觉统一与氛围增强

- [ ] 顶部导航为铺满式顶栏，不再是悬浮胶囊主结构。
- [ ] 向下滚动时顶栏平滑隐藏，向上滚动时重新显示。
- [ ] 鼠标移动到页面顶部区域时顶栏重新显示。
- [ ] 移动端导航折叠可用，不撑宽页面。
- [ ] 首页第一排为“个人名片 + 音乐播放器”双主区，1024px 以下可合理变为单列。
- [ ] 首页第二排为全宽歌词/播放状态区，并能显示当前歌曲或占位信息。
- [ ] 首页第三排包含最新文章、最新更新内容和昼夜模式切换卡片。
- [ ] 最新更新内容来自公开 posts/moments/chatters 聚合，不显示草稿。
- [ ] 昼夜模式切换卡片可一键切换日间/夜间模式，刷新后保留本地偏好。
- [ ] 底部状态区显示北京时间、网站运行时间和 Vue 3 / Vite / Tailwind CSS / FastAPI 技术栈。
- [ ] 设置中心“主题与背景”支持字体、字号、日间 token、夜间 token 表单配置，保存后前台刷新生效。
- [ ] 首页 1440px、1280px、1024px、390px 下 ProfileCard、SiteDashboard、最新文章、内容发现入口完整可见，不撑宽、不裁切。
- [ ] 首页 4 篇及以上文章时最新文章网格正常换行，左右边距稳定。
- [ ] 首页按钮、标签、统计卡片、最新文章卡片 hover 轻微且不造成布局抖动。
- [ ] 文章详情标题、日期、标签、摘要、正文、分享和评论区层级清楚。
- [ ] Markdown 正文、代码块、表格、图片在 390px 下可读；代码块/表格只在自身内部横向滚动。
- [ ] `/search`、`/tags`、`/archive` 卡片和状态块没有被视觉改动破坏。
- [ ] `/friends`、`/projects`、`/music`、`/photowall` 卡片 hover、图片兜底和点击区域正常。
- [ ] 背景控制里的“氛围”开关可关闭樱花、萤火、CyberCat 和点击光效。
- [ ] 关闭氛围动效后，页面仍保持基本美观，正文不受影响。
- [ ] 移动端默认不显示高密度装饰层，弹幕/背景不遮挡正文和按钮。
- [ ] 后台 `/admin/`、`/admin/editor`、`/admin/comments`、`/admin/backups`、`/admin/settings` 控制台卡片、侧边栏 active、危险按钮和表单 focus 状态清楚。
- [ ] 后台审计日志和备份恢复仍保持工具风，不出现花哨动效或主流程被弱化。
- [ ] Tab 聚焦、按钮 aria-label、关闭按钮、禁用状态没有因视觉改动倒退。
- [ ] 构建产物 Secret 搜索通过。

## 前台文章详情

- [ ] `/posts/srblogs-p0-20260502130609` 可打开。
- [ ] `/posts/vue-fastapi-blog` 可打开。
- [ ] 标题、日期、标签、摘要正确。
- [ ] Markdown 正文正常。
- [ ] 代码块高亮正常。
- [ ] TOC 显示并可点击滚动。
- [ ] 图片加载失败有兜底。
- [ ] 分享按钮点击不报错。
- [ ] 不存在 slug 显示 404/错误状态，不白屏。
- [ ] 390px 移动端可读。

## 前台首页响应式

- [ ] 1440px：首页 Hero 和 ProfileCard 正常并排，右侧不裁切，无横向滚动条。
- [ ] 1280px：ProfileCard 不被裁切，SiteDashboard 统计卡片不溢出，顶部导航不溢出。
- [ ] 1024px：首页 Hero 和 ProfileCard 自动变为单列或合理压缩，fixed 控件不遮挡内容，无横向滚动条。
- [ ] 390px：首页可读可操作，导航可操作，统计卡片换行，右侧/底部浮动控件不挡正文和按钮。
- [ ] 1440px、1280px、1024px、390px 下主内容左右边距基本一致，页面不能视觉上向右顶出。
- [ ] 顶部导航、Hero/ProfileCard、SiteDashboard、LatestPosts 使用同一主内容宽度，不出现导航居中但首页右贴边。
- [ ] 右侧主题/背景/弹幕按钮不遮挡 ProfileCard 或主要内容。
- [ ] FloatingPlayer 不遮挡文章内容、评论区和主要按钮。
- [ ] 页面整体无横向滚动条，图片和卡片最大宽度不超过 viewport。
- [ ] 即使没有横向滚动条，也不能出现 ProfileCard、头像、昵称、统计、社交按钮或 SiteDashboard 最右侧卡片被裁切；出现裁切即为不通过。
- [ ] 首页“最新文章”不是横向滚动轨道；4 篇文章在当前主容器宽度下应换到第二行第一列，而不是继续排成第一行第四列。
- [ ] 文章数量从 1、2、3、4、5 变化时，首页主容器宽度和左右边距不跳变。

## 全站路由回归

- [ ] `/` 可打开，不白屏。
- [ ] `/posts` 可打开，有加载、空、错误状态。
- [ ] `/posts/:slug` 可打开真实文章；不存在 slug 显示 404/错误状态。
- [ ] `/moments` 可打开，有加载、空、错误状态。
- [ ] `/chatters` 可打开，有加载、空、错误状态。
- [ ] `/chatters/:slug` 可打开真实杂谈；不存在 slug 显示 404/错误状态。
- [ ] 旧路径 `/chatter` 和 `/chatter/:slug` 会重定向到 `/chatters` 和 `/chatters/:slug`。
- [ ] `/friends`、`/projects`、`/music`、`/photowall` 可打开，不白屏。
- [ ] `/about` 可打开并渲染 `about.md`；加载失败有 UI 错误。
- [ ] `/timeline` 可打开，有加载、空、错误状态。
- [ ] 不存在的前台路径显示 404 页面，不白屏。
- [ ] 390px 宽度下主要页面没有严重横向溢出。
- [ ] sticky 顶部导航不遮挡正文。
- [ ] 背景弹幕/气泡不得覆盖主要阅读区域。

## 内容发现与归档

- [ ] `/search` 可打开，不白屏。
- [ ] `/search?q=vue&type=posts` URL query 与搜索框/类型筛选同步。
- [ ] 搜索结果显示类型、标题、摘要、标签、日期，并可点击跳转。
- [ ] 不存在关键词显示空状态。
- [ ] `/tags` 可打开并显示标签数量。
- [ ] `/tags/:tag` 可打开并展示该标签下的内容。
- [ ] 不存在标签显示空状态，不白屏。
- [ ] `/archive` 可打开并按年份、月份显示 posts/moments/chatters。
- [ ] 归档项显示日期、类型、标题和标签，并可点击跳转。
- [ ] 首页有搜索、标签、归档和最近更新入口。
- [ ] 390px 宽度下搜索、标签、归档页面不出现严重横向溢出。

## SEO、订阅与分享

- [ ] 首页、文章列表、动态、杂谈、友链、项目、音乐、照片墙、关于、时间线、搜索、标签、归档、404 页面有合理 title 和 description。
- [ ] 文章详情 title、description、OpenGraph 和 Twitter Card 随文章标题、摘要、封面变化。
- [ ] 不存在 slug 时 title 显示 404 或内容不存在。
- [ ] `GET /api/rss.xml` 可在浏览器打开，且不包含草稿文章。
- [ ] `GET /api/sitemap.xml` 可在浏览器打开，且不包含草稿文章。
- [ ] `GET /robots.txt` 可在浏览器打开，并包含 `Disallow: /admin`。
- [ ] Sitemap 包含公开固定路由、已发布文章详情、杂谈详情和标签详情。
- [ ] 前台文章列表或关于页能看到 RSS 入口。
- [ ] 文章详情点击“复制链接”显示成功提示。
- [ ] 复制失败时有可读失败提示。
- [ ] 构建产物不包含 Secret。

## 性能、可访问性与体验稳定

- [ ] 首页、文章列表、文章详情、搜索、标签、归档、友链、项目、音乐、照片墙、关于页均有加载/空/错误状态，API 失败不白屏。
- [ ] 文章封面、头像、项目封面、友链头像、音乐封面、照片墙图片均有 `alt` 或可读替代说明。
- [ ] 非首屏图片使用懒加载；图片加载失败时显示兜底图，不撑爆布局。
- [ ] `/posts/not-exist` 或其他不存在 slug 显示 404/内容不存在，不白屏。
- [ ] Markdown 代码块和表格允许自身横向滚动，但不撑宽整个页面。
- [ ] 390px 下 `/`、`/posts/:slug`、`/search`、`/tags`、`/archive`、`/photowall` 不出现严重横向滚动。
- [ ] 390px 下 `/admin/editor`、`/admin/settings`、`/admin/comments` 可滚动操作，侧栏不遮挡主内容。
- [ ] 照片墙放大预览可用关闭按钮关闭，也可按 Esc 关闭。
- [ ] 评论表单昵称、邮箱、内容输入可通过键盘 Tab 聚焦；提交中按钮禁用，不能重复提交。
- [ ] 前台导航菜单、背景控制、悬浮播放器、分享按钮均可通过键盘聚焦并有可读名称。
- [ ] 后台保存/删除/上传失败时有页面可见提示，不只在 console 报错。
- [ ] 前台背景光效和弹幕/气泡不遮挡主要阅读区域；移动端动效密度不会影响阅读。
- [ ] `frontend/dist` 和 `admin/dist` 构建体积已检查；超过 500 kB 的 Markdown 相关 chunk 已记录且不进入首屏主包。

## 后台路由回归

- [ ] `/admin/` 可从后台导航进入。
- [ ] `/admin/editor` 可从后台导航进入。
- [ ] `/admin/posts` 可从后台导航进入。
- [ ] `/admin/comments` 可从后台导航进入。
- [ ] `/admin/friends`、`/admin/projects`、`/admin/music`、`/admin/photos` 可从后台导航进入。
- [ ] `/admin/settings` 可从后台导航进入。
- [ ] `/admin/audit` 可从后台导航进入。
- [ ] `/admin/backups` 可从后台导航进入。
- [ ] 未登录或登录过期时有明确跳转或提示。
- [ ] API 失败时显示 UI 错误，不只在 console 报错。
- [ ] 表单保存成功/失败提示清楚。
- [ ] 删除类操作都有确认。
- [ ] 高级 JSON 编辑不作为默认主流程。

## 后台审计日志与备份恢复

- [ ] `/admin/audit` 默认显示审计日志列表。
- [ ] 审计日志支持 action、resource 和关键词筛选。
- [ ] 审计日志显示时间、操作者、动作、资源、目标、结果、说明。
- [ ] 失败日志有明显状态。
- [ ] 日志列表有加载、空、错误状态。
- [ ] 日志中不显示 Secret 明文。
- [ ] 新建文章、删除评论、修改 settings、上传文件、备份、恢复、导入、导出等高风险动作能产生审计记录。
- [ ] `/admin/backups` 默认显示备份列表。
- [ ] 点击“创建手动备份”后按钮禁用并显示处理中，成功后列表刷新。
- [ ] 备份列表显示文件名、创建时间、文件大小。
- [ ] 下载备份可用，且接口需要 JWT。
- [ ] 恢复备份前出现二次确认，文案包含“恢复会覆盖当前 backend/data 内容，系统会先创建恢复前备份。”
- [ ] 恢复成功后 `.manual_backups` 中新增 `pre-restore-*.zip`。
- [ ] 备份 zip 不包含 `.env`、前端源码、`node_modules`、`dist` 或 Secret 明文。

## 发布候选与生产部署演练

- [ ] `backend/.env.production.example` 不包含真实 Secret，并明确 `ADMIN_PASSWORD`、`JWT_SECRET` 生产必须修改。
- [ ] `deploy/build-all.sh` 能构建前台、后台并执行后端语法检查。
- [ ] `deploy/start-backend.sh` 使用 `/etc/srblogs/backend.env` 或指定 `ENV_FILE` 启动后端。
- [ ] `deploy/srblogs-backend.service` 不包含本地 Windows 路径，包含 `WorkingDirectory`、`EnvironmentFile`、`ExecStart`、`Restart=always`、`User`。
- [ ] `deploy/nginx.srblogs.conf` 包含前台静态、后台静态、`/api` 反代、`/uploads`、`/robots.txt`、gzip、缓存和上传大小限制。
- [ ] `deploy/healthcheck.sh` 能检查后端 health、前台、后台、RSS、Sitemap 和 robots。
- [ ] `GET /api/admin/system/status` 登录后可访问，不返回 Secret。
- [ ] `docs/PRODUCTION_CHECKLIST.md` 覆盖构建、环境变量、Secret、CORS、Nginx、systemd、RSS/Sitemap/robots、上传、审计、备份、恢复和回滚。
- [ ] 下载/恢复路径穿越名称被拒绝。
- [ ] 导出数据 zip 可下载。
- [ ] 导入 zip 前有二次确认，导入失败时显示 UI 错误。

## 后台 Markdown 编辑器

- [ ] 点击“插入预览测试样例”后，右侧预览是真实 Markdown 渲染，不是纯文本。
- [ ] 一级标题明显大于二级标题，二级标题明显大于三级标题。
- [ ] 无序列表显示项目符号。
- [ ] 有序列表显示 1、2、3 编号。
- [ ] blockquote 有引用样式。
- [ ] inline code 有等宽字体和背景。
- [ ] fenced code block 有等宽字体、背景和横向滚动。
- [ ] table 显示为表格，窄屏不横向撑破页面。
- [ ] link 有链接样式。
- [ ] image 最大宽度不超过预览区。
- [ ] 预览仍经过 DOMPurify 清洗。

## 前台评论区

- [ ] 评论列表加载状态可见。
- [ ] 空状态可见。
- [ ] 错误状态可见。
- [ ] 昵称不能为空。
- [ ] 评论内容不能为空。
- [ ] 邮箱可为空。
- [ ] 非法邮箱有提示。
- [ ] 提交成功后列表无刷新更新。
- [ ] 长评论换行正常。
- [ ] XSS 内容不被危险渲染。

## 后台评论管理

- [ ] `/admin/comments` 可进入。
- [ ] 重启后端后，`http://127.0.0.1:8000/docs` 中能看到 `GET /api/admin/comments/index`。
- [ ] 登录后直接请求 `GET /api/admin/comments/index` 返回 200，不再是 404。
- [ ] 页面打开后不需要手动输入 slug。
- [ ] 默认显示有评论的内容索引。
- [ ] 索引项显示标题、resource、slug、评论数和最近更新时间。
- [ ] 点击索引项后自动加载对应评论。
- [ ] 当前选中项有 active 状态。
- [ ] 删除评论成功。
- [ ] 删除不存在评论显示错误。
- [ ] 删除后前台评论区同步变化。
- [ ] 删除后评论列表数量减少，索引 count 同步刷新。
- [ ] 删除前 `backend/data/comments/.backups` 生成备份。
- [ ] 手动 resource/slug 加载只在“高级加载”折叠区中出现，不作为主流程。

## 后台写作

- [ ] `/admin/posts` 可进入。
- [ ] 文章列表显示标题、slug、日期、标签、draft 状态、更新时间。
- [ ] 文章列表支持按标题搜索。
- [ ] 文章列表支持按 slug 搜索。
- [ ] 全部/已发布/草稿筛选正确。
- [ ] 点击“编辑”进入写作页并加载该文章 Markdown 正文和元信息。
- [ ] “前台预览”能打开对应前台详情地址。
- [ ] 后台新建非草稿文章。
- [ ] 文件写入 `backend/data/posts`。
- [ ] 前台文章列表出现。
- [ ] 前台文章详情可打开。
- [ ] 后台新建草稿文章后，`backend/data/posts` 有文件，但前台文章列表不显示。
- [ ] 草稿入口可看到 draft=true 文章。
- [ ] 草稿可继续编辑。
- [ ] 草稿“立即发布”后 draft=false，前台文章列表出现，前台详情可打开。
- [ ] 已发布文章“设为草稿”后，前台文章列表消失，公开详情返回 404 或错误状态。
- [ ] 草稿“发布加入暂存”不会立刻写后端，点击暂存区“应用”后才发布。
- [ ] 编辑已发布文章后，保存成功提示可见，刷新前台详情可看到更新。
- [ ] 删除文章前有确认。
- [ ] 删除文章后 `backend/data/posts/.backups` 生成备份。
- [ ] 删除文章后前台文章列表不再显示该文章。
- [ ] 删除文章后前台详情显示 404/错误状态。
- [ ] 非法 slug 的 UI 错误提示可见。
- [ ] 空 slug 的 UI 错误提示可见。
- [ ] 空标题的 UI 错误提示可见。
- [ ] 重复 slug 的 UI 错误提示可见。
- [ ] 网络错误或保存失败时 UI 显示可读错误，不只在控制台报错。
- [ ] 保存成功时 UI 显示成功提示。
- [ ] 页面明确区分“保存”直接持久化和“加入暂存”本地 pendingOperations。

## 后台暂存队列第一阶段

- [ ] 写作页点击“加入暂存”后不立刻写入后端。
- [ ] 右侧“操作暂存区”显示待执行操作列表。
- [ ] 每条操作显示操作类型、目标标题、slug、状态、创建时间。
- [ ] 队列状态只使用 `editing`、`pending`、`applied`、`failed`。
- [ ] 点击“应用”后才调用真实后端 API 写入。
- [ ] 点击“一键应用全部”后会逐项调用真实后端 API。
- [ ] 应用成功后状态变为 `applied`。
- [ ] 应用失败后状态变为 `failed`，并显示错误信息。
- [ ] failed 操作可以重试或移除。
- [ ] pending 操作可以移除。
- [ ] 页面明确提示第一阶段为前端本地队列，刷新页面会丢失。
- [ ] 文章新建可进入暂存并应用成功。
- [ ] 文章编辑可进入暂存并应用成功。
- [ ] 文章删除可进入暂存并应用成功，删除前有备份。
- [ ] 草稿发布可进入暂存并应用成功。
- [ ] 非 Secret 设置修改可进入暂存并应用成功。
- [ ] 图片上传、Secret 修改、评论管理不进入本地 pendingOperations。

## 媒体与结构化内容管理

- [ ] `/admin/friends` 主流程是表单化管理，不是默认 JSON 文本框。
- [ ] 友链可新增、编辑、删除，字段包含名称、URL、描述、头像/图标 URL、标签。
- [ ] 新增友链后前台 `/friends` 出现，删除后消失。
- [ ] `/friends` 有加载状态、空状态、错误状态；链接在新标签页打开。
- [ ] `/admin/projects` 主流程是表单化管理。
- [ ] 项目可新增、编辑、删除，字段包含名称、描述、技术栈、项目链接、仓库链接、封面 URL、状态。
- [ ] 新增项目后前台 `/projects` 出现，删除后消失。
- [ ] `/projects` 有加载状态、空状态、错误状态；项目链接和仓库链接在新标签页打开。
- [ ] `/admin/music` 主流程是表单化管理。
- [ ] 歌曲可新增、编辑、删除，字段包含歌曲标题、艺术家、封面 URL、歌曲 URL 或云音乐 ID、排序。
- [ ] 新增歌曲后前台 `/music` 出现，页面播放器和悬浮播放器能读取歌单数据。
- [ ] 删除歌曲后前台 `/music` 更新。
- [ ] `/music` 有加载状态、空状态、错误状态。
- [ ] `/admin/photos` 主流程是表单化管理。
- [ ] 照片可新增、编辑、删除，字段包含图片 URL、标题、描述、日期、标签。
- [ ] 照片管理可调用上传接口，上传成功后 URL 自动填入表单。
- [ ] 新增照片后前台 `/photowall` 出现，删除后消失。
- [ ] `/photowall` 图片懒加载，点击图片可放大预览，并有加载状态、空状态、错误状态。
- [ ] friends/projects/music/photos 保存后重新读取 API，确认数据已更新。
- [ ] 每次覆盖写入前对应 `.backups` 目录生成备份。
- [ ] 高级 JSON 编辑在折叠区内，格式错误或根节点不是数组时阻止保存并显示错误。
- [ ] 图片上传、Secret 修改、评论删除没有进入本地 pendingOperations。

## 设置中心与生产化

- [ ] `/admin/settings` 可进入。
- [ ] 设置中心默认是分区表单主流程，不是单一 JSON 文本框。
- [ ] 页面包含站点公开信息、主题与背景、评论设置、图床设置、AI 设置、部署与安全提示。
- [ ] 加载状态、保存中状态、保存成功提示、保存失败提示均可见。
- [ ] 修改站点标题后保存，刷新前台首页能看到同步变化。
- [ ] 修改作者、头像 URL、站点简介、社交链接后，前台资料卡能同步变化或有兜底。
- [ ] 背景图列表保存后前台能读取；空 URL 或失效图片不导致白屏。
- [ ] `GET /api/settings/public` 只包含公开站点配置，不包含 AI Key、OSS Key、GitHub OAuth Secret、JWT、管理员密码。
- [ ] `GET /api/admin/settings` 需要登录 JWT。
- [ ] 后台设置接口只显示 `xxxConfigured` 布尔值，不回显 Secret 明文。
- [ ] Secret 输入框留空保存时，旧 Secret 不被覆盖。
- [ ] 输入新 Secret 保存后，重新读取只看到 configured=true，不看到明文。
- [ ] 关闭评论后，前台文章详情显示“评论已关闭。”且不显示提交框。
- [ ] 重新开启评论后，前台文章详情评论提交框恢复。
- [ ] 评论最大长度、是否要求邮箱等前台校验跟随公开评论设置。
- [ ] 图床 provider、publicBaseUrl、bucket、region、endpoint 可保存。
- [ ] 图床 AccessKey/Secret 不回显明文。
- [ ] 图床“测试配置”按钮有可读结果；本地上传接口仍校验类型、MIME 和大小。
- [ ] AI provider、baseUrl、model、enableChat 可保存。
- [ ] AI API Key 不回显明文；前台永远拿不到 AI Key。
- [ ] 高级 JSON 编辑只在折叠区内，JSON 非对象时阻止保存并显示错误。
- [ ] `docs/DEPLOYMENT.md` 包含 FastAPI 启动、前端 build、后台 build、Nginx、systemd、`backend/data` 权限、`.env`、HTTPS 和生产前检查。
- [ ] Windows 启动文档中的固定地址、设置中心地址和 Secret 边界说明可执行。
- [ ] 生产前检查完成：`ADMIN_PASSWORD` 已修改、`JWT_SECRET` 已修改、CORS 白名单已收紧、开发端口不暴露、构建产物不含 Secret、`backend/data` 已备份、上传大小限制有效。
- [ ] `frontend/dist` 和 `admin/dist` 静态搜索不包含 Secret 字段名、默认密码或真实 Secret 值。

## 最终总回归与真实部署准备

- [ ] 前台主路径 `/`、`/posts`、`/posts/:slug`、`/moments`、`/chatters`、`/chatters/:slug`、`/friends`、`/projects`、`/music`、`/photowall`、`/about`、`/timeline`、`/search`、`/tags`、`/tags/:tag`、`/archive` 和不存在路由均不白屏。
- [ ] 后台主路径 `/admin/`、`/admin/editor`、`/admin/posts`、`/admin/comments`、`/admin/friends`、`/admin/projects`、`/admin/music`、`/admin/photos`、`/admin/settings`、`/admin/audit`、`/admin/backups` 均能从导航进入。
- [ ] 完整内容生产演示流通过：登录后台、新建草稿、发布、前台详情、提交评论、后台删除评论、编辑、撤回发布、删除文章、审计日志、手动备份、下载备份。
- [ ] 手动备份 zip 不包含 `.env`，恢复前自动创建 `pre-restore-*.zip`。
- [ ] `GET /api/settings/public` 和 `GET /api/admin/settings` 不暴露 Secret 明文。
- [ ] `frontend/dist` 和 `admin/dist` 构建产物不包含当前服务端 Secret 值。
- [ ] `backend/.env.production.example` 明确要求生产修改 `ADMIN_PASSWORD` 和 `JWT_SECRET`，且不建议生产 `CORS_ORIGINS=*`。
- [ ] `deploy/nginx.srblogs.conf` 包含前台、后台、`/api/`、`/uploads/`、`client_max_body_size`。
- [ ] `deploy/srblogs-backend.service` 和 deploy 脚本不包含本机 Windows 绝对路径。
- [ ] 真实服务器、域名和 HTTPS 实操如未执行，应在矩阵中标记为 `部署实操待执行`，不得标记为完全完成。
- [ ] P2 视觉增强按本轮视觉回归清单继续推进，不引入大型动画库、不做 3D、不破坏 P0/P1。
## 2026-05-03 Album Modal Scroll Focused QA

- [ ] `/admin/photos` create album modal is constrained to the viewport and has a visible modal-body scrollbar when content is long.
- [ ] `/admin/photos` edit album modal can scroll through all album fields and all album photos.
- [ ] Album photos area does not have its own clipped fixed-height preview panel; the photo grid grows naturally.
- [ ] Photo thumbnails keep stable preview size and do not let large images stretch the modal.
- [ ] Save/clear/close controls remain reachable after scrolling.
- [ ] Batch upload, delete photo, move/sort, drag/drop, and set cover still work.
- [ ] 390px width can still scroll and operate the album modal.

## 2026-05-03 Multi-provider Message QA

- [ ] Article detail message board shows GitHub and QQ login choices when logged out.
- [ ] Chatter detail message board shows GitHub and QQ login choices when logged out.
- [ ] Music page shows a `music/global` message board without breaking playback, lyrics, playlist, or volume.
- [ ] Photowall album dialog shows a per-album message board without breaking preview, sorting, cover, or upload workflows.
- [ ] GitHub unavailable text is visitor-friendly and does not mention backend, `.env`, or Secret.
- [ ] QQ unavailable text is visitor-friendly and does not mention backend, `.env`, or Secret.
- [ ] Anonymous `POST /api/comments/{resource}/{slug}` returns 401.
- [ ] Search inputs are about 75% of the previous height and more opaque on desktop and 390px mobile.
# 2026-05-03 多平台留言与中文化回归补充

- [ ] 前台文章详情留言板不出现英文提示。
- [ ] GitHub 已配置时显示“使用 GitHub 登录后留言”按钮。
- [ ] QQ 未配置时只显示 QQ 未开启提示，不影响 GitHub 按钮。
- [ ] GitHub / QQ 登录按钮点击后访问 `/api/auth/{provider}/login?returnTo=当前路径`，不出现 Not Found。
- [ ] 未登录直接提交留言显示中文错误提示。
- [ ] 后台设置中心留言设置、主题与背景、图床、AI、部署提示均为中文展示。
- [ ] 后台 Secret 状态显示“已配置 / 未配置”，不显示 true/false。
- [ ] 搜索框更不透明，且高度比上一轮更低；390px 下仍可输入和点击搜索图标。
